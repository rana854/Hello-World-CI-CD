apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deployment5
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp5
  template:
    metadata:
      labels:
        app: myapp5
    spec:
      containers:
      - name: myapp-container5
        image: "ranatarek/pipline_docker_image33:latest"
        ports:
        - containerPort: 8000
